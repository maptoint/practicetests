package oca;

/*

Assuming we have a valid, non-null HenHouse object whose value is
initialized by the blank line shown here, which of the following
are possible outputs of this application? (Choose all that apply)

A. The code will not compile because of line 6.

B. The code will not compile because of lines 7-8.

C. The application will compile but not produce any output.

D. The application will output Click exactly once.

E. The application will output Click more than once.

F. The application will compile but produce an exception at runtime.

 */
public class JavaOCA014 {
}
/*

class Chicken {}
interface HenHouse { public java.util.List<Chicken> getChickens(); }
class ChickenSong {
    public static void main(String[] args) {
        HenHouse house = __________
                Chicken chicken = house.getChickens().get(0);
        for(int i = 0; i < house.getChickens().size()) {
            System.out.println("Cluck");
        }
    }
}

*/
